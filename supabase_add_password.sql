-- =====================================================
-- إضافة عمود exam_password لجدول exams
-- شغّل هذا في Supabase SQL Editor
-- =====================================================

-- إضافة عمود كلمة المرور (إذا كانت الجداول موجودة مسبقاً)
ALTER TABLE exams
ADD COLUMN IF NOT EXISTS exam_password TEXT DEFAULT '';

-- =====================================================
-- مثال: تعيين كود/باسورد لامتحان موجود
-- =====================================================
-- UPDATE exams SET exam_password = 'FR2025' WHERE exam_code = 'EXAM-1';

-- =====================================================
-- ملاحظة: إذا كنت تستخدم الـ Schema الجديد (supabase_new_schema.sql)
-- أضف exam_password مباشرة في جدول exams:
--
-- exam_password TEXT DEFAULT '',
-- =====================================================
